{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "checkJs": false,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },
    "types": ["node", "jest"]
  },
  "exclude": ["node_modules"]
}

// disabled nodemodules to check typescript

// inside package.json

// "format": "prettier --write '**/*.{js,ts,jsx,tsx}'",
// "format:check": "prettier --check '**/*.{js,ts,jsx,tsx}'"
// to avoid conflict for config
// eslint-config-prettier install this package
